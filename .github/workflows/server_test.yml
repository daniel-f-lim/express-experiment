name: Test Express Server
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout
      - name: Install Dependencies
        run: npm ci
      - name: Run Tests
        run: npm test